services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: procura
      POSTGRES_PASSWORD: procura
      POSTGRES_DB: procura
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U procura"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
